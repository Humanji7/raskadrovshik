# Инструкция по деплою на Vercel

## Что было сделано для безопасности

### ✅ Проблема решена
API ключ Gemini теперь **НЕ** встраивается в клиентский код. Все запросы к Gemini API теперь идут через серверные Vercel Serverless Functions.

### Изменения:
1. **Создана папка `/api`** с тремя serverless functions:
   - [generate-description.ts](api/generate-description.ts) - генерация описания из эскиза
   - [generate-storyboards.ts](api/generate-storyboards.ts) - генерация 4 раскадровок
   - [edit-image.ts](api/edit-image.ts) - редактирование изображения

2. **Обновлен [services/geminiService.ts](services/geminiService.ts)**:
   - Удалены прямые обращения к Gemini API
   - Теперь делает fetch запросы к нашим API endpoints

3. **Обновлен [vite.config.ts](vite.config.ts)**:
   - Убрали `define` секцию с API ключом
   - Ключ больше не встраивается в клиентский код

4. **Обновлен [package.json](package.json)**:
   - Добавлен `@vercel/node` для типов Vercel functions

## Пошаговый деплой

### 1. Установи зависимости (если еще не делал)
```bash
npm install
```

### 2. Инициализируй Git (если еще не сделал)
```bash
git init
git add .
git commit -m "Initial commit: Раскадровщик курильщика с безопасным API"
```

### 3. Создай репозиторий на GitHub
```bash
# Создай репо на github.com, потом:
git remote add origin https://github.com/твой-username/твой-репо.git
git branch -M main
git push -u origin main
```

### 4. Деплой на Vercel

#### Вариант А: Через веб-интерфейс
1. Зайди на [vercel.com](https://vercel.com)
2. Нажми "Add New Project"
3. Import твой GitHub репозиторий
4. Vercel автоматически определит что это Vite проект
5. **ВАЖНО**: В разделе "Environment Variables" добавь:
   ```
   Name: GEMINI_API_KEY
   Value: AIzaSyAH_2b-ZLAq8d_InMUvSObOmkWgiCXf694
   ```
6. Нажми "Deploy"

#### Вариант Б: Через CLI
```bash
npm i -g vercel
vercel login
vercel

# Добавь environment variable:
vercel env add GEMINI_API_KEY
# Вставь свой ключ когда попросит
```

### 5. Готово! 🎉

Твое приложение задеплоится на адрес типа: `https://твой-проект.vercel.app`

## Важные моменты

### ✅ Безопасность
- API ключ хранится **только** на серверах Vercel
- Клиентский код **не содержит** ключа
- Пользователи **не могут** украсть ключ из браузера

### ⚙️ Как это работает
```
Браузер → /api/generate-storyboards → Vercel Function → Gemini API
                                      (с API ключом)
```

### 🔄 Обновления
После изменений в коде:
```bash
git add .
git commit -m "твоё сообщение"
git push
```
Vercel автоматически задеплоит новую версию.

### 🐛 Если что-то не работает

1. **Проверь Environment Variable в Vercel Dashboard**:
   - Settings → Environment Variables
   - Должна быть `GEMINI_API_KEY`

2. **Проверь логи**:
   - В Vercel Dashboard → твой проект → Functions
   - Там будут логи серверных функций

3. **Локальный тест** (опционально):
   ```bash
   vercel dev
   # Проверь что всё работает локально
   ```

## Что дальше?

### Опциональные улучшения:
- Добавить rate limiting (ограничение запросов)
- Добавить аналитику использования
- Кеширование результатов
- Добавить прелоадер/прогресс бар для длительных генераций

### Структура после деплоя:
```
/
├── api/                         # Serverless Functions (бэкенд)
│   ├── generate-description.ts
│   ├── generate-storyboards.ts
│   └── edit-image.ts
├── services/                    # Frontend сервисы
│   └── geminiService.ts         # Обращается к /api/*
├── components/                  # React компоненты
├── App.tsx                      # Главный компонент
└── dist/                        # Build output (создается при деплое)
```

Удачи! 🚬
